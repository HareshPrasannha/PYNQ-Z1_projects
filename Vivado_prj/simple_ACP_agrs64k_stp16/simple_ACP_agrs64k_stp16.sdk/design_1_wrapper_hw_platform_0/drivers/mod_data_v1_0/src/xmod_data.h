// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.3
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XMOD_DATA_H
#define XMOD_DATA_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xmod_data_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Axilites_BaseAddress;
} XMod_data_Config;
#endif

typedef struct {
    u32 Axilites_BaseAddress;
    u32 IsReady;
} XMod_data;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XMod_data_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XMod_data_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XMod_data_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XMod_data_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XMod_data_Initialize(XMod_data *InstancePtr, u16 DeviceId);
XMod_data_Config* XMod_data_LookupConfig(u16 DeviceId);
int XMod_data_CfgInitialize(XMod_data *InstancePtr, XMod_data_Config *ConfigPtr);
#else
int XMod_data_Initialize(XMod_data *InstancePtr, const char* InstanceName);
int XMod_data_Release(XMod_data *InstancePtr);
#endif

void XMod_data_Start(XMod_data *InstancePtr);
u32 XMod_data_IsDone(XMod_data *InstancePtr);
u32 XMod_data_IsIdle(XMod_data *InstancePtr);
u32 XMod_data_IsReady(XMod_data *InstancePtr);
void XMod_data_EnableAutoRestart(XMod_data *InstancePtr);
void XMod_data_DisableAutoRestart(XMod_data *InstancePtr);
u32 XMod_data_Get_return(XMod_data *InstancePtr);

void XMod_data_Set_a(XMod_data *InstancePtr, u32 Data);
u32 XMod_data_Get_a(XMod_data *InstancePtr);

void XMod_data_InterruptGlobalEnable(XMod_data *InstancePtr);
void XMod_data_InterruptGlobalDisable(XMod_data *InstancePtr);
void XMod_data_InterruptEnable(XMod_data *InstancePtr, u32 Mask);
void XMod_data_InterruptDisable(XMod_data *InstancePtr, u32 Mask);
void XMod_data_InterruptClear(XMod_data *InstancePtr, u32 Mask);
u32 XMod_data_InterruptGetEnabled(XMod_data *InstancePtr);
u32 XMod_data_InterruptGetStatus(XMod_data *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
