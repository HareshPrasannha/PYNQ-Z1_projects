// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.3
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "ADDR_BUS"
#define AUTOTB_TVIN_ADDR_BUS  "../tv/cdatafile/c.poll_ocm.autotvin_ADDR_BUS.dat"
#define AUTOTB_TVOUT_ADDR_BUS  "../tv/cdatafile/c.poll_ocm.autotvout_ADDR_BUS.dat"
// wrapc file define: "addr"
#define AUTOTB_TVIN_addr  "../tv/cdatafile/c.poll_ocm.autotvin_addr.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "ADDR_BUS"
#define AUTOTB_TVOUT_PC_ADDR_BUS  "../tv/rtldatafile/rtl.poll_ocm.autotvout_ADDR_BUS.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			ADDR_BUS_depth = 0;
			addr_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{ADDR_BUS " << ADDR_BUS_depth << "}\n";
			total_list << "{addr " << addr_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int ADDR_BUS_depth;
		int addr_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void poll_ocm (
int* addr,
int* data);

void AESL_WRAP_poll_ocm (
int* addr,
int* data)
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "ADDR_BUS"
		aesl_fh.read(AUTOTB_TVOUT_PC_ADDR_BUS, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_ADDR_BUS, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_ADDR_BUS, AESL_token); // data

			sc_bv<32> *ADDR_BUS_pc_buffer = new sc_bv<32>[1];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'ADDR_BUS', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'ADDR_BUS', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					ADDR_BUS_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_ADDR_BUS, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_ADDR_BUS))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: ADDR_BUS
				{
					// bitslice(31, 0)
					// {
						// celement: addr(31, 0)
						// {
							sc_lv<32>* addr_lv0_0_0_1 = new sc_lv<32>[1];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: addr(31, 0)
						{
							// carray: (0) => (0) @ (1)
							for (int i_0 = 0; i_0 <= 0; i_0 += 1)
							{
								if (&(addr[0]) != NULL) // check the null address if the c port is array or others
								{
									addr_lv0_0_0_1[hls_map_index++].range(31, 0) = sc_bv<32>(ADDR_BUS_pc_buffer[hls_map_index].range(31, 0));
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: addr(31, 0)
						{
							// carray: (0) => (0) @ (1)
							for (int i_0 = 0; i_0 <= 0; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : addr[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : addr[0]
								// output_left_conversion : addr[i_0]
								// output_type_conversion : (addr_lv0_0_0_1[hls_map_index++]).to_uint64()
								if (&(addr[0]) != NULL) // check the null address if the c port is array or others
								{
									addr[i_0] = (addr_lv0_0_0_1[hls_map_index++]).to_uint64();
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] ADDR_BUS_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "ADDR_BUS"
		char* tvin_ADDR_BUS = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_ADDR_BUS);
		char* tvout_ADDR_BUS = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_ADDR_BUS);

		// "addr"
		char* tvin_addr = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_addr);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_ADDR_BUS, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_ADDR_BUS, tvin_ADDR_BUS);

		sc_bv<32>* ADDR_BUS_tvin_wrapc_buffer = new sc_bv<32>[1];

		// RTL Name: ADDR_BUS
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: addr(31, 0)
				{
					// carray: (0) => (0) @ (1)
					for (int i_0 = 0; i_0 <= 0; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : addr[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : addr[0]
						// regulate_c_name       : addr
						// input_type_conversion : addr[i_0]
						if (&(addr[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> addr_tmp_mem;
							addr_tmp_mem = addr[i_0];
							ADDR_BUS_tvin_wrapc_buffer[hls_map_index++].range(31, 0) = addr_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_ADDR_BUS, "%s\n", (ADDR_BUS_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_ADDR_BUS, tvin_ADDR_BUS);
		}

		tcl_file.set_num(1, &tcl_file.ADDR_BUS_depth);
		sprintf(tvin_ADDR_BUS, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_ADDR_BUS, tvin_ADDR_BUS);

		// release memory allocation
		delete [] ADDR_BUS_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_addr, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_addr, tvin_addr);

		sc_bv<32> addr_tvin_wrapc_buffer;

		// RTL Name: addr
		{
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_addr, "%s\n", (addr_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_addr, tvin_addr);
		}

		tcl_file.set_num(1, &tcl_file.addr_depth);
		sprintf(tvin_addr, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_addr, tvin_addr);

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		poll_ocm(addr, data);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_ADDR_BUS, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_ADDR_BUS, tvout_ADDR_BUS);

		sc_bv<32>* ADDR_BUS_tvout_wrapc_buffer = new sc_bv<32>[1];

		// RTL Name: ADDR_BUS
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: addr(31, 0)
				{
					// carray: (0) => (0) @ (1)
					for (int i_0 = 0; i_0 <= 0; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : addr[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : addr[0]
						// regulate_c_name       : addr
						// input_type_conversion : addr[i_0]
						if (&(addr[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> addr_tmp_mem;
							addr_tmp_mem = addr[i_0];
							ADDR_BUS_tvout_wrapc_buffer[hls_map_index++].range(31, 0) = addr_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvout_ADDR_BUS, "%s\n", (ADDR_BUS_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_ADDR_BUS, tvout_ADDR_BUS);
		}

		tcl_file.set_num(1, &tcl_file.ADDR_BUS_depth);
		sprintf(tvout_ADDR_BUS, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_ADDR_BUS, tvout_ADDR_BUS);

		// release memory allocation
		delete [] ADDR_BUS_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "ADDR_BUS"
		delete [] tvin_ADDR_BUS;
		delete [] tvout_ADDR_BUS;
		// release memory allocation: "addr"
		delete [] tvin_addr;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

